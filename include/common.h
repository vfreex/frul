#ifndef FRUL_COMMON_H
#define FRUL_COMMON_H

#ifndef max
#define max(a, b) \
    ( (a) > (b) ? (a) : (b) )
#endif

#ifndef min
#define min(a, b) \
    ( (a) < (b) ? (a) : (b) )
#endif

#endif //FRUL_COMMON_H
